<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mgb.manipalglobalbank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_manipal_orange"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".LatestTransactionActivity" />
        <activity android:name=".StatementActivity" />
        <activity android:name=".TransferFundsActivity" />
        <activity android:name=".UtilityPaymentActivity"></activity>
    </application>

</manifest>